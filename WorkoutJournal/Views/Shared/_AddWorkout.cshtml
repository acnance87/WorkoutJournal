@using WorkoutJournal.Data.Models
@*
    For more information on enabling MVC for empty projects, visit http://go.microsoft.com/fwlink/?LinkID=397860
*@

@model IEnumerable<WorkoutTypes>

@foreach (var exercise in Model) {
    <input type="checkbox" name="workoutsSelected" class="workoutSelect form-control checkbox-inline" id="@($"{exercise.WorkoutTypeID}-{exercise.ExerciseName}")" />
    <label for="@($"{exercise.WorkoutTypeID}-{exercise.ExerciseName}")">@exercise.ExerciseName</label>
    <br />
}
